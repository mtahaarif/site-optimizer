node_modules/
.next/
*.tsbuildinfo
next-env.d.ts

# Secrets
.env
.env.local

# Leftover local state from the pre-Postgres version (old SQLite db + disk
# cache). Nothing in the app writes here anymore; kept ignored so it never
# gets swept into a commit by accident.
.data/

# Research corpus (179 downloaded HTML files)
research/issues/
research/*.html
research/*.xml
